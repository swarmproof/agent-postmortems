schema_version: "1.0"
incident_id: 2099-invalid-type
title: "Fixture: wrong type for amount_usd"
date: "2099-01-01"
incident_type: incident
system:
  framework: fixture
primary_failure_class: runaway-loop
failure_classes:
  - class: runaway-loop
    subclass: retry-storm
trigger: "A fixture trigger condition."
root_cause: "A fixture systemic root cause."
detection: "Noticed by a fixture monitor."
recovery: "Stopped by a fixture kill switch."
prevention: "A fixture loop budget."
blast_radius:
  scope: "none - test fixture"
  cost:
    amount_usd: "about a thousand"
sources:
  - url: "https://example.com/fixture"
    type: other
